# =============================================================================
# Configurazione progetto WordPress (Docker)
# Copia in .env e adatta a ambiente locale o produzione.
# Con progetto vuoto: configura .env e lancia ./restore.sh per ripristinare un backup.
# =============================================================================

# --- Database (MariaDB) ---
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

# --- WordPress ---
# Porta su cui espone il container (es. 8080 in locale, 80 dietro proxy in prod)
WORDPRESS_PORT=

# URL con cui userai il sito (opzionale ma consigliato per restore).
# In restore viene usato come URL NUOVO predefinito quando ripristini un backup
# fatto in locale su un altro ambiente (es. produzione).
# Esempi: http://localhost:8080 (sviluppo) | https://test.com (produzione)
# SITE_URL=https://test.com

# Debug WordPress (true/false o vuoto)
# WORDPRESS_DEBUG=

# --- Opzionale: chiavi e salt (per nuove installazioni) ---
# Per generare: https://api.wordpress.org/secret-key/1.1/salt/
# Se non impostate, l'immagine Docker usa valori di default (ok per dev; in prod è meglio definirle).
# WORDPRESS_AUTH_KEY=
# WORDPRESS_SECURE_AUTH_KEY=
# WORDPRESS_LOGGED_IN_KEY=
# WORDPRESS_NONCE_KEY=
# WORDPRESS_AUTH_SALT=
# WORDPRESS_SECURE_AUTH_SALT=
# WORDPRESS_LOGGED_IN_SALT=
# WORDPRESS_NONCE_SALT=

# --- Opzionale: prefisso tabelle (default wp_) ---
# WORDPRESS_TABLE_PREFIX=wp_