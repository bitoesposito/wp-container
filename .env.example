# =============================================================================
# WordPress project configuration (Docker)
# Copy to .env and adapt for local or production environments.
# One WordPress project = one repo folder + one dedicated .env file.
# With an empty project: configure .env and run ./restore.sh to restore a backup.
# =============================================================================

# --- Database (MariaDB) ---
# Each project should use its own database/user/password
# when you have multiple sites on the same VM.
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

# --- WordPress ---
# Port exposed by the container (e.g. 8080 locally, 80 behind a proxy in prod).
# If you have multiple projects on the same VM, use a different port for each one.
WORDPRESS_PORT=

# URL used to access the site (optional but **recommended** for restore/migrations).
# It is used as the NEW URL default when restoring a backup created locally
# into another environment (e.g. production).
# Examples:
#  - development: http://localhost:8080
#  - production:  https://client-1-site.example.com
# Set a different value for each project when you have multiple sites on the same VM.
# SITE_URL=https://test.com

# WordPress debug (true/false or empty)
# WORDPRESS_DEBUG=

# --- Optional: keys and salts (for new installs) ---
# Generate from: https://api.wordpress.org/secret-key/1.1/salt/
# If not set, the Docker image uses default values (fine for dev; in prod you should define them).
# WORDPRESS_AUTH_KEY=
# WORDPRESS_SECURE_AUTH_KEY=
# WORDPRESS_LOGGED_IN_KEY=
# WORDPRESS_NONCE_KEY=
# WORDPRESS_AUTH_SALT=
# WORDPRESS_SECURE_AUTH_SALT=
# WORDPRESS_LOGGED_IN_SALT=
# WORDPRESS_NONCE_SALT=

# --- Optional: table prefix (default wp_) ---
# WORDPRESS_TABLE_PREFIX=wp_